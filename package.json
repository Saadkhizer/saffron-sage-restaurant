{
  "name": "restaurant-app",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack restaurant ordering website (frontend + backend)",
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c green,cyan \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "seed": "cd backend && npm run seed",
    "build": "cd frontend && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
